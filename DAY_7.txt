import java.util.*;
import java.util.stream.*;

public class Main
{
	public static void main(String[] args) {

	//1. Find the Sum of All Odd Numbers in a Nested List

     List<List<Integer>> listOfLists = List.of(
                                        List.of(1, 2, 3),
                                        List.of(4, 5, 6),
                                        List.of(7, 8, 9)
                                    );
	int sum = listOfLists
	            .stream()
	            .flatMap(List::stream)
	            .filter(n -> n%2 == 0)
	            .reduce(0,(a,b) -> a+b);
     //	System.out.println(sum);

	//2. Find the Longest Palindrome in a List of Strings

	List<String> words = List.of("madam", "racecar", "apple", "banana", "level");
	String longestPolindrome = words
	                           .stream()
	                           .filter(s->s.equals (new StringBuilder(s).reverse().toString()))
	                           .max(Comparator.comparingInt(String::length))
	                           .orElse("");
	  //System.out.println(longestPolindrome);

	//3.shortest  polindrome 

	String shortestPolindrome = words
	                            .stream()
	                            .filter(s->s.equals(new StringBuilder(s).reverse().toString()))
	                            .min(Comparator.comparingInt(String::length))
	                            .orElse("");
	//System.out.println(shortestPolindrome);

	//4. Find the Longest Word in a String

	String input = "java is a object oriented language";
	String longestWord = Arrays.stream(input.split("\\s+"))
	                     .max(Comparator.comparingInt(String::length))
	                     .orElse("");
    //System.out.println(longestWord);

    //5.shortes Word

    String shortestWord = Arrays.stream(input.split("\\s+"))
                         .min(Comparator.comparingInt(String::length))
                         .orElse("");
    //System.out.println(shortestWord);

	//6.Find the Number of Words in a String

	long numberOfWordsInString = Arrays.stream(input.split("\\s+"))
	                           .count();
	//System.out.println(numberOfWordsInString);
	
	}
}