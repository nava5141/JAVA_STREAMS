import java.util.*;
import java.util.stream.*;
//2
class Person{
    String name;
    int age;
    Person(String name,int age){
        this.name= name;
        this.age= age;
    }
    int getAge(){
        return this.age;
    }
}
//3
class Hello{
    public static boolean isPrime(int n){
        if(n<=1) return false;
        for(int i=2;i<=Math.sqrt(n);i++){
            if(n%i==0){
                return false;
            }
        }
        return true;
    }
}
public class Main
{
	public static void main(String[] args) {
	//Map
	List<Integer> list = Arrays.asList(1,3,4,2,5);
	List<Integer> list1= list
	                    .stream()
	                    .map(i->i+2)
	                    .collect(Collectors.toList());
	  //System.out.println(list1);
	  //Filter
            List<Integer>list2=list
                	  .stream()
                	  .filter(i->i%2==0)
                	 .collect(Collectors.toList());
                	 // System.out.println(list2);
    //Sorted    
          List<Integer>list3=      list
                .stream()
                .sorted()
                .collect(Collectors.toList());
        // System.out.println(list3);
    //Count
    long divBy3 = list
                    .stream()
                    .filter(i->i%3==0)
                    .count();
            //System.out.println(divBy3);
            
    //1.Find longest string in the list of strings
        List<String>str = Arrays.asList("banana","tamato","grapefruit","date");
        Optional<String>longestString = str
                                        .stream()
                                        .max(Comparator.comparingInt(String::length));
        //System.out.println(longestString.get());//Here .get() used for print direct output instead of Optional
     
     //2.Calculate the average age of list of persons
        List<Person>persons= Arrays.asList(
                    new Person("anil",24),
                    new Person("dan",25));
            double averageAge = persons
                                .stream()
                                .mapToInt(Person::getAge)
                                .average()
                                .orElse(0);
               // System.out.println(averageAge);   
               
    //3.Check if a list of integers contains a prime numbers or not           
         boolean containsPrime = list
                                    .stream()
                                    .anyMatch(Hello::isPrime);
                     //   System.out.println(containsPrime);    
                     
    //4.Merge two sorted lists into a single sorted list
    List<Integer> arr1= Arrays.asList(1,2,4,5);
    List<Integer>arr2 = Arrays.asList(3,4,6);
    List<Integer> mergedLst = Stream.concat(arr1.stream(),arr2.stream())
                                .sorted()
                                .collect(Collectors.toList());
                               // System.out.println(mergedLst);
                               
    //5.Find the intersection of 2 lists
        List<Integer>intersection = arr1
                                    .stream()
                                    .filter(arr2::contains)
                                    .collect(Collectors.toList());
                                   // System.out.println(intersection);
    //6.Remove duplicates from List
        List<Integer>dup = Arrays.asList(1,2,2,3,3,4,5);
        List<Integer>noDup = dup
                            .stream()
                            .distinct()
                            .collect(Collectors.toList());
                         //   System.out.println(noDup);
               
	}
}